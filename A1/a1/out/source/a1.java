/* autogenerated by Processing revision 1295 on 2025-02-27 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.Arrays;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class a1 extends PApplet {



int noiseSeed = 1337;
float noiseScale = 0.005f;
float noiseXBias = 0.25f;
float noiseYBias = 1.00f;
int numOctaves = 5;
float threshold = 0.225f;
float octaveBase = 2.4f;
boolean shouldThreshold = true;
boolean binaryThreshold = true;
int peakOctaveLimit = 2;

public int IX(int x, int y) { return y * width + x; }

OpenSimplex2S noiseGen;

public void setup()
{
  /* size commented out by preprocessor */;
}

public void generateShardNoise(int[] arr, int nOctaves, float scale, float xBias, float yBias, float threshold, float amplitudeBase, int peakOctaves, long seed) {
  float offset = random(100);
  for(int y = 0; y < height; y++) {
    for(int x = 0; x < width; x++) {
      float accum = 0.0f;
      for(int o = 0; o < nOctaves; o++) {
        //float n = OpenSimplex2S.noise2(noiseSeed, x * noiseScale, y * noiseScale);
        float xscale = (o > peakOctaves) ? xBias : 1;
        float n = OpenSimplex2S.noise2(seed,
                                       x * pow(2, o) * scale * xscale + offset,
                                       y * pow(2, o) * scale * yBias + offset);      
        float intensity;
        if( o <= peakOctaves) intensity = map(n, -1.0f, 1.0f, 0.0f, 255.0f);
        else intensity = abs(n) * 255.0f;
        accum += pow(amplitudeBase, -(o + 1)) * intensity;
      }
      float finalIntensity = accum;
      //if(shouldThreshold) {
      if(threshold > 0.0f) {
        if((accum < (threshold * 255.0f))) {
          finalIntensity = (binaryThreshold) ? 255.0f : accum;
        } else {
          finalIntensity = 0.0f;      
        }
      }
      arr[IX(x, y)] = color(finalIntensity);
    }
  }
}

public void generateSpecularNoise(int[] arr, int nOctaves, float scale, int seed) {
  for (int y = 0; y < height; y++) {
    for (int x = 0; x < width; x++) {
      float accum = 0.0f;
      float noiseSum = 0.0f;

      for (int o = 0; o < nOctaves; o++) {
        float freq = pow(2, o) * scale * 4;
        float weight = 1.0f;
        float n = OpenSimplex2S.noise2(seed, x * freq, y * freq);
        noiseSum += weight * n;
      }
      
      float baseIntensity = map(noiseSum, -1.0f, 1.0f, 20, 150); // Cloudy background

      // Add sharp stars with high contrast
      float grain = OpenSimplex2S.noise2(seed + 100, x * scale * 8, y * scale * 8);
      if (grain > 0.7f) {
        baseIntensity = 255; // bright
      } else if (grain > 0.4f) {
        baseIntensity = map(grain, 0.7f, 0.85f, baseIntensity, 255); // Mid-intensity
      }

      arr[IX(x, y)] = color(baseIntensity);
    }
  }
}

public void draw()
{
  
  loadPixels();
  Arrays.fill(pixels, color(0, 0, 0));
  updatePixels();
  loadPixels();
   
  int[] staging = new int[pixels.length];
  int[] shards = new int[pixels.length]; 
  int[] shardEdges = new int[pixels.length];
  int[] shardGradients = new int[pixels.length];
  int[] smallCrackTexture = new int[pixels.length];
  int[] smallCrackTextureAcross = new int[pixels.length];
  int[] largeCrackTexture = new int[pixels.length];
  int[] rotatedShards = new int[pixels.length]; 
  int[] rotatedCracks = new int[pixels.length]; 
  int[] texturedShards = new int[pixels.length];
  int[] baseColor = new int[pixels.length];
  int[] edgeColor = new int[pixels.length];

  int[] backgroundNoise = new int[pixels.length];

  //Arrays.fill(baseColor, color(81, 75, 63));
  Arrays.fill(baseColor, color(93, 89, 78));
  Arrays.fill(edgeColor, color(245, 246, 232));
  
  int numLayers = 21;
  for(int layer = 0; layer < numLayers; layer++) {
    Arrays.fill(shardEdges, color(0, 0, 0));
    Arrays.fill(shardGradients, color(0, 0, 0));
    Arrays.fill(smallCrackTexture, color(0, 0, 0));
    Arrays.fill(smallCrackTextureAcross, color(0, 0, 0));
    Arrays.fill(largeCrackTexture, color(0, 0, 0)); 
    Arrays.fill(rotatedCracks, color(0, 0, 0)); 
    Arrays.fill(texturedShards, color(0, 0, 0));
    
    float rotation = random(-PI/4, PI/4);
    
    // BASE SHARD SHAPES
    int seed = PApplet.parseInt(random(100000000));
    generateShardNoise(shards, 5, 0.005f, 0.25f, 1.00f, 0.225f, 2.4f, 1, PApplet.parseInt(random(100000000)));
    applySobelGradient(shards, shardGradients);
    
    // SHARD TEXTURE
    // large cracks
    generateShardNoise(largeCrackTexture, 5, 0.0085f, 0.4f, 12.05f, 0.385f, 2.4f, -1, PApplet.parseInt(random(100000000)));
    invertColors(largeCrackTexture);
  
    // small cracks texture
    binaryThreshold = false;
    generateShardNoise(smallCrackTexture, 5, 0.03f, 0.4f, 7.05f, 0.3f, 2.4f, -1, PApplet.parseInt(random(100000000)));
    binaryThreshold = true;
    //invertColors(smallCrackTexture);

    generateShardNoise(staging, 5, 0.03f, 9.05f, 0.4f, 0.45f, 2.4f, -1, PApplet.parseInt(random(100000000)));
    invertColors(staging);
    rotateImage(staging, smallCrackTextureAcross, random(-PI/6, PI/6)); 

    
    // combine cracks into ont texture
    addBlend(largeCrackTexture, smallCrackTexture, 0.7f);
    addBlend(largeCrackTexture, smallCrackTextureAcross, 0.5f);

        
    // apply texture to inside of shards
    maskLayer(shards, baseColor, texturedShards, 1.0f);
    maskLayer(shards, largeCrackTexture, texturedShards, 1.75f);
    
    // apply jitter edges to shard texture
    jitterEdges(shardGradients, edgeColor, shardEdges);
    addBlend(texturedShards, shardEdges, 1.7f);
    
    // ROTATE SHARDS
    rotateImage(texturedShards, rotatedShards, rotation); 

    float alpha = 1.0f * ((1.0f/(numLayers*1.5f)) * (layer + 1));
    // mask out areas that already have many shards to avoid too much overlap
    arrayCopy(pixels, staging);
    invertColors(staging);
    maskLayer(staging, rotatedShards, pixels, alpha, 0.8f);

    println("Layer:", layer+1);
    
  }
  
  arrayCopy(pixels, staging);
  invertColors(staging);  
  generateSpecularNoise(backgroundNoise, 4, 0.01f, PApplet.parseInt(random(1000000)));
  maskLayer(staging, backgroundNoise, pixels, 0.1f);
  
  updatePixels();
  println("DONE\n");
}

/* UTILS */

public void jitterEdges(int[] input, int[] layer, int[] output) {
  int seed = PApplet.parseInt(random(100000));
  float noiseX = random(100);
  for(int y = 10; y < height-1; y++) {
    for(int x = 10; x < width-1; x++) {
      int sample = input[IX(x, y)];
      
      int start;
      int end;
      if(red(sample) != 127) {
        float maxJitter = 15.0f;
        float noise = -1 * abs(OpenSimplex2S.noise2(seed, noiseX, y*0.1f)) + 0.5f; // only partial shift
        int r = PApplet.parseInt(noise * maxJitter);
        // jitter inwards towards the shard contents
        if(red(sample) < 127) {
          start = -r;
          end = 0;
        } else {
          start = 1;
          end = r + 1;
        }
        
        for(int j = start; j < end; j++) {
          int shiftedix = IX(constrain(x + j, 0, width - 1), y);
          output[shiftedix] = layer[shiftedix];
          //output[shiftedix] = color(255);
        }
      }
    }
  }
}

public void applyConvolution(int[] pixelArray, float[][] kernel, int[] result) {
  int w = width, h = height;
  int kw = kernel.length, kh = kernel[0].length;
  int kCenterX = kw / 2, kCenterY = kh / 2;

  for (int y = kCenterY; y < h - kCenterY; y++) {
    for (int x = kCenterX; x < w - kCenterX; x++) {
      float r = 0, g = 0, b = 0;
      for (int ky = 0; ky < kh; ky++) {
        for (int kx = 0; kx < kw; kx++) {
          int px = (x + kx - kCenterX) + (y + ky - kCenterY) * w;
          int col = pixelArray[px];
          float weight = kernel[ky][kx];
          r += red(col) * weight;
          g += green(col) * weight;
          b += blue(col) * weight;
        }
      }
      int i = x + y * w;
      result[i] = color(constrain(r, 0, 255), constrain(g, 0, 255), constrain(b, 0, 255));
    }
  }
}

public void applySobelGradient(int[] input, int[] result) {
  int w = width, h = height;

  float[][] sobelX = {
    {-1, 0, 1},
    {-2, 0, 2},
    {-1, 0, 1}
  };

  float[][] sobelY = {
    {-1, -2, -1},
    { 0,  0,  0},
    { 1,  2,  1}
  };

  for (int y = 1; y < h - 1; y++) {
    for (int x = 1; x < w - 1; x++) {
      float gx = 0, gy = 0;

      for (int ky = -1; ky <= 1; ky++) {
        for (int kx = -1; kx <= 1; kx++) {
          int px = (x + kx) + (y + ky) * w;
          float intensity = brightness(input[px]);

          gx += intensity * sobelX[ky + 1][kx + 1];
          gy += intensity * sobelY[ky + 1][kx + 1];
        }
      }

      // map gradients to 0-255 range
      int r = PApplet.parseInt(map(gx, -255, 255, 0, 255));
      int b = PApplet.parseInt(map(gy, -255, 255, 0, 255));
      result[x + y * w] = color(r, 0, b);
    }
  }
}

public void maskLayer(int[] mask, int[] layer, int[] output, float alpha) {
  for(int y = 0; y < height; y++) {
    for(int x = 0; x < width; x++) {
      int i = IX(x, y);
      if(mask[i] == color(255)) {
        output[i] = addColors(output[i], layer[i], 1.0f, alpha);
      }
    }
  }
}

public void maskLayer(int[] mask, int[] layer, int[] output, float alpha, float maskIntensity) {
  for(int y = 0; y < height; y++) {
    for(int x = 0; x < width; x++) {
      int i = IX(x, y);
      if(mask[i] > color(255 * maskIntensity)) {
        output[i] = addColors(output[i], layer[i], 1.0f, alpha);
      }
    }
  }
}

public void addBlend(int[] a, int[] b, float alpha) {
  for(int i = 0; i < a.length; i++) {
    a[i] = addColors(a[i], b[i], 1.0f, alpha);
  }
}

public void addBlendAlpha(int[] a, int[] b, float a1, float a2) {
  for(int i = 0; i < a.length; i++) {
    a[i] = addColors(a[i], b[i], a1, a2);
  }
}

public int addColors(int c1, int c2, float a1, float a2) {
  int r = PApplet.parseInt(constrain(red(c1) * a1 + red(c2) * a2, 0, 255));
  int g = PApplet.parseInt(constrain(green(c1) * a1 + green(c2) * a2, 0, 255));
  int b = PApplet.parseInt(constrain(blue(c1) * a1 + blue(c2) * a2, 0, 255));
  
  return color(r, g, b);
}

public void invertColors(int[] pixelArray) {
  for (int i = 0; i < pixelArray.length; i++) {
    pixelArray[i] = color(255 - red(pixelArray[i]), 255 - green(pixelArray[i]), 255 - blue(pixelArray[i]));
  }
}

public void rotateImage(int[] input, int[] output, float angle) {
  float cx = (width - 1) / 2.0f;  // Center X
  float cy = (height - 1) / 2.0f;  // Center Y
  
  float cosA = cos(angle);
  float sinA = sin(angle);

  for (int y = 0; y < width; y++) {
    for (int x = 0; x < height; x++) {
      float nx = x - cx;
      float ny = y - cy;

      // apply inverse rotation
      int srcX = round(cx + (nx * cosA - ny * sinA));
      int srcY = round(cy + (nx * sinA + ny * cosA));

      if (srcX >= 0 && srcX < width && srcY >= 0 && srcY < height) {
        output[IX(x, y)] = input[IX(srcX, srcY)];
      } else {
        output[IX(x, y)] = color(0); // fill with black if out of bounds
      }
    }
  }
}

/* CONTROLS */
public void mouseWheel(MouseEvent event) {
  float scrollSens = 0.001f;
  float scroll = event.getCount();
  noiseScale += scrollSens * scroll;
}

public void keyPressed() {
  if (keyCode == 'Q') numOctaves = max(1, numOctaves - 1);
  if (keyCode == 'W') numOctaves += 1;
  if (keyCode == 'E') threshold -= 0.025f;
  if (keyCode == 'R') threshold += 0.025f;
  if (keyCode == 'T') noiseXBias -= 0.05f;
  if (keyCode == 'Y') noiseXBias += 0.05f;
  if (keyCode == 'U') noiseYBias -= 0.05f;
  if (keyCode == 'I') noiseYBias += 0.05f;
  if (keyCode == 'A') octaveBase -= 0.05f;
  if (keyCode == 'S') octaveBase += 0.05f;
  if (keyCode == 'D') peakOctaveLimit -= 1;
  if (keyCode == 'F') peakOctaveLimit += 1;
  if (keyCode == '1') shouldThreshold = !shouldThreshold;
  if (keyCode == '0') noiseSeed = PApplet.parseInt(random(100000000));
  //if (keyCode == 'P') {
  // 12.050028 0.3999998   0.0029999996   2.4   -1   0.35000002 5
  println(noiseXBias, noiseYBias, " ", noiseScale, " ", octaveBase, " ", numOctaves, peakOctaveLimit, " ", threshold);
  println(noiseXBias, noiseYBias, " ", noiseScale, " ", octaveBase, " ", numOctaves, peakOctaveLimit, " ", threshold);
}


  public void settings() { size(800, 800); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "a1" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
